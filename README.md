# Personal

Quick start guide:
```bash
./scripts/run.sh up          # start (from repo root)
./scripts/run.sh restart     # rebuild and restart
./scripts/lint.sh            # lint/format check (CI)
./scripts/lint.sh --really   # apply fixes locally
./scripts/test.sh            # run pytest
```
